var n = 15;
n *=10; //Essa operação é igual a fazer "n = n - 10"
console.log(n);

var x = 10;
x++; //Esse comando "++" quer dizer que tá adicionando 1 a variável
console.log(x);

a = 5
b = 15;
C = 60;

console.log(n <= x && b != C); //a operação "&&" significa condição "E", a operação "!=" siginifica diferente de

console.log(n <= x || b != C); //o comando || significa OU.

//Verificar se a é maior do que b e b é divisível por 2
console.log(a > b && b % 2 == 0);//o comando "%" significa resto de uma divisão ou se ele é divisível por tal número. Divide dois números e retorna o restante.

//verificar se b é maior igual a ou b dividido por 2 é 2.
console.log(b >= a || b / 2 == 2); //o comando "==" retorna verdadeiro se alguma coisa for igual outra.

//verificar se a é maior que b e b maior igual a 5
console.log(a > b && b >= 5);

//verificar se b é par p ou impar usando ternario (?) ternário é o interrogação
resultado = b % 2 == 0 ? "par" : "impar"; //os dois pontos ":" significa "se não".

